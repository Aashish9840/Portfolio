# Contact form delivery (https://resend.com). Copy to .env.local and fill in.
RESEND_API_KEY=
# Inbox that receives portfolio messages
CONTACT_TO_EMAIL=
# Optional: a sender on a domain verified in Resend. Defaults to onboarding@resend.dev
CONTACT_FROM_EMAIL=
